apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: app
  namespace: app-prod
  labels: { app: app }
spec:
  to:
    kind: Service
    name: app
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
